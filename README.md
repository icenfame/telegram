# Telegram React Native with Expo

- Install packages: `npm install`
- Rename `.env.example` to `.env` and edit with your [Telegram credentials](https://my.telegram.org/apps)
- Start expo: `npm start`
